[tool.pytest.ini_options]
pythonpath = ['.']
testpaths = ['tests']

[tool.ruff]
target-version = 'py312' # Keep local preview compatible; Docker runs Python 3.14.7.
line-length = 100

[tool.ruff.lint]
select = ['E4', 'E7', 'E9', 'F', 'I']
